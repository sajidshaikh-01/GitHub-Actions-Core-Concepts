name: Genrate ASCII Art
on: [push]

jobs:
  generate-ascii-art:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: list files in the repository
        run: ls -ltra

      - name: Executing shell script to generate ASCII art
        run: ./.github/script.sh